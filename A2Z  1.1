// this is output in java 

public class Main {
public static void main(String... args){
System.out.println("this line is output):
}
}

//output is python

print("this is output")


//not output along with input in java
import java.util.*;
public class Main{
public static void main (String... args){
Scanner sc = new Scanner(System.in);

System.out.println("enter your age");
int age = sc.nextInt();

System.out.println("your age is " + age);

sc.close();
}
}

//java code with input output 
import java.util.*;

public class Main {
    public static void main (String... args){
        
        Scanner sc = new Scanner(System.in);
        
        System.out.println("what is your name ?");
        String name = sc.nextLine();
        
        System.out.println("what your age");
        int age = sc.nextInt();
        
        System.out.print("your name is :" + name  + "your age is" +age);
        sc.close();
    }
}

//now python code with input and output
print("program for your name") 
name = input("what is your name ")
age = int(input("what is your age boi"))

print("your name is  {name} and your age is {age}")
//or
print("your name is" , name , "and your age is " , age)



// this was input out put in java now lets move to next sheet


// now we have data types sheet 
byte	1 byte	0	    byte b = 100;
short	2 bytes	0	    short s = 2000;
int  	4 bytes	0	    int num = 50000;
long	8 bytes	0L	    long bigNum = 100000L;
float	4 bytes	0.0f	float pi = 3.14f;
double	8 bytes	0.0d	    double e = 2.718;
char	2 bytes	'\u0000'	char ch = 'A';
boolean	1 bit	false	boolean flag = true;


//sheet 3 if else statement

//java
int n = 10;
if(n>10){
System.out.println("number is greater than 10"):
}
{
else("number is less")
}

// in python
number = 50
if number > 50:
    print("wwe was emotion back then")
else:
    print("i still love gambling")

//sheet 4 switch statement in java and python

import java.util.*;

public class Main {
    public static void main (String... args){
       int day= 5;
       
       switch(day){
           case 1 :
               System.out.println("monday");
               break;
            case 2 :
               System.out.println("tuesday");
               break;
           case 3:
               System.out.println("wednesday");
               break;
           case 4: 
               System.out.println("thursday");
               break;
            case 5:
               System.out.println("friday");
               break;
            case 6:
               System.out.println("saturday");
               break;
       }

    }
}
//switch is not in java but ham use karenge elif
num = 69 
if num == 5:
    print("eaah")
elif num ==56:
     print("eureka")
elif num ==69: 
    print("chud gaye guru")
    
 //now for loop in java and python sheet 5
//java
 for(int i = 0 ; i < 5 ; i++){
 System.out.println("numbers" +i);}
 //python
 for num in range(69):
     print(num)


//sheet 6 while loops in java and python
// java while loop
import java.util.*;

public class Main {
    public static void main (String... args){
      
      int count = 0;

while (count < 5) {
    System.out.println("Count: " + count);
    count++; // Increment the count
}    }
}

 //python while loop
 count = 0

while(count < 5):
    print(count )
    count+=1    //count++ is not supported in python it uses count+=1

//this is all in this sheet now lets move to sheet 1.2

// Time complexity : This is not the time required to run a code , this is actually something different . so what time complexity is that the rate of change of time required to run a code with change of input size is time complexity.To represent the time complexity, we generally use the Big O notation.

//space complexity generally represents the summation of auxiliary space and the input space. Auxiliary space refers to the space that we use additionally to solve a problem. And input space refers to the space that we use to store the inputs.

int a = 5 //input space 
int b = 6 //input space
int c ;
c = a + b ; // auziliary space




























